# venv core — orchestration + rendu + interface (aucun modèle IA)
# numpy : borné sous Linux (compat opencv testée) ; libre sous Windows où
# seuls les wheels récents existent pour les Python récents (cf. install.ps1).
numpy<2.3 ; sys_platform != "win32"
numpy>=1.24 ; sys_platform == "win32"
opencv-python>=4.9
imageio-ffmpeg>=0.5
tqdm>=4.66
gradio>=5
psutil>=5.9        # moniteur CPU/RAM de l'interface
nvidia-ml-py>=12   # moniteur GPU/VRAM (inoffensif sans GPU NVIDIA)
